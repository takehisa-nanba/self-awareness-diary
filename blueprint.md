# アプリケーション設計書: 自己覚知日記（研磨日記）

## 概要
ユーザーが日々の出来事を「原石」として記録し、自己覚知という「砥石」で磨き上げるプロセスを支援する。AIはユーザーの知覚の「粒度（解像度）」を測定・向上させる相棒であり教師である。

## コア・コンセプト：砥石の粒度
- **自己覚知の粒度**: 感情をどれだけ微細に、客観的に捉えられるかの指標。
- **研磨のプロセス**: 日記を書くことで砥石を使い、診断で粒度を再調整（キャリブレーション）する。

## 世界観の統合：日記から宇宙への「昇華」 (4次元宇宙への世界観統合)
このアプリは「紙の日記帳」というメタファーから、**「自分という宇宙を観測する記録装置」というメタファーへ進化しました。** 日記を書く行為を「原石の採掘」、分析を「宇宙の観測」として再定義し、UI/UX全体でこの世界観を表現しています。

### 1. 共通背景アーキテクチャ
- **universe_background.dart**: アプリ全体の背景として、深海グラデーション、ゆっくり動く星間ガス（Nebula）、瞬く星々（Stars）の3層構造を持つ再利用可能なウィジェットを実装。

### 2. スプラッシュから診断への「ワープ遷移」
- **brand_splash_screen.dart**: スプラッシュ画面にUniverseBackgroundを統合し、診断画面への遷移時に星々が画面中央から奥へ吸い込まれるようなワープ演出（Curves.easeInExpo、1.2秒）を実装。`TickerProviderStateMixin` への修正によりアニメーションコントローラーの衝突問題も解消。
  - タイトル「Min(e) Diary」のアニメーションを修正。`SingleTickerProviderStateMixin` が `TickerProviderStateMixin` に変更され、複数のアニメーションコントローラーが正しく動作するようになりました。また、単純な文字列処理ではなく、`RichText` と `WidgetSpan` を組み合わせた物理的な隙間制御を導入し、(e) の出現時の不要な空白を解消しました。`mindStyle`を「Min」と「(e)」と「D」に、`normalDiaryStyle`を「iary」に適用し、(e)の左右に適切なパディングを設定しました。アニメーションは「Min」→「Diary」（「D」が太字）→「(e)」の順で展開され、不自然な隙間なく文字が密着するよう調整されました。さらに、「Min」と「(e)」、「(e)」と「D」の間にスペースを追加し、視覚的なバランスを向上させました。
  - 診断完了状態に基づいた条件付きナビゲーションを実装。性格診断が未完了の場合のみ診断画面へ遷移し、完了している場合はメインのホーム画面へ直接遷移するようになりました。
- **universe_canvas.dart**: `_flareAnimationController` の `LateInitializationError` を解消するため、`initState`での初期化と`dispose`での破棄処理を徹底しました。
- **main.dart**: `InitialScreenWrapper` の表示ロジックを `settings.isFirstLaunch` から `!settings.isDiagnosisComplete` に変更し、診断が未完了の場合にスプラッシュ画面を表示するようにしました。

### 3. 宇宙的診断UI（53の問い）のデザイン
- **_QuestionCard (Glassmorphism)**: 各質問カードは `BackdropFilter` を使用したグラスモフィズム効果（背景透明度0.05、ぼかしsigmaX/Y: 10、枠線 Color(0xFF4FD1C5).withOpacity(0.3)）で実装。選択された回答ボタンは背景色と文字色が動的に変化し、視覚的に強調されるようになりました。
- **_ConstellationProgressBar (星座の形成)**: 画面上部に53個のドットで進捗を表示。回答に応じて点が輝き、前の星と細い線で繋がることで星座が形成されるプロセスを表現。（星座線や最終アニメーションは今後のイテレーションで実装予定。）
- **診断画面の表示改善**: 「性格診断」タイトルは大きく明るく、その下に「(?/53)」形式で現在の質問進捗を表示し視認性を向上。質問回答ボタンのレイアウトもユーザーフィードバックに基づき改善済み（はい/-1/どちらでもない）。全ての質問に回答が完了すると「コンプリート」ボタンが有効化され、サイズと色が変化して視覚的にアピールします。
- **未回答設問への誘導**: 「コンプリート」ボタン押下時に未回答の設問がある場合、未回答の設問のリストをダイアログで提示し、そこから選択して移動できるように、または強制的に最初の未回答設問へジャンプできるように改善しました。最終確定前には確認のポップアップが表示されます。

### 4. 診断ロジックの強化
- **DiagnosisProvider**: TEG 3（東大式エゴグラム）に準拠した53問の質問（CP, NP, A, FC, AC）を実装。（質問内容はAIが生成した汎用的なもの。）
- **スコア計算**: 質問への回答（はい: +1, いいえ: -1, どちらでもない: 0）を各指標にマッピングし、100点満点で正規化するロジックを実装。

### 5. 既存コードの尊重と拡張
- **CosmicInterpretationService/CosmicObservation**: `DiaryRecord` や `polishingLevel` のロジックは変更せず、その数値出力を宇宙の「物理パラメータ（輝き、距離、深度）」として解釈するための新しいレイヤー（`CosmicObservation` モデルと `CosmicInterpretationService`）を実装。
- **app_theme.dart**: 宇宙背景に適した `universeTheme` を追加定義し、アプリのテーマ管理に統合。`ColorScheme` のdeprecatedメンバーの使用を修正済み。
- **IsarService**: `getDiaryRecordsForDateRange` メソッドを追加し、Isarフィルターの正確な使用法に準拠するように修正。

## 教師・相棒からのフィードバックと今後の改善提案
### 1. 背景の連続性
- **現状**: スプラッシュから診断画面への `pushReplacement` 遷移時、`UniverseBackground` も新しく生成されるため、星の位置がリセットされる可能性があります。
- **提案**: 将来的には、アプリのルート（AppShell など）に `UniverseBackground` を配置し、画面切り替え時に背景が途切れないようにすることで、より没入感のある「旅」の体験を提供できます。

### 2. 星の瞬きの負荷
- **解決済**: 1つの `AnimationController` で「時間の進み」だけを管理し、`CustomPainter` 内で `sin(time + 星の個性)` のように計算して瞬かせることで、より滑らかで効率的なアニメーションを実現しました。

## 実装計画：精密診断とティア別深化

### パフォーマンス改善の実施 (完了)
- **UniverseBackground のリファクタリング**: 200個の星の瞬きを個別の AnimationController ではなく、単一の CustomPainter 内の時間計算（sin(time + seed)) で完結するように変更しました。
- **UniverseCanvas の最適化**:
    - `RepaintBoundary` の導入: `UniverseCanvas` の `CustomPaint` を `RepaintBoundary` で包み、不要な再描画を抑制しました。
    - アニメーション制御の分離: 視点回転や拡大縮小の処理において、`ValueNotifier` と `ValueListenableBuilder` を使用し、`Painter` の更新を必要最小限の範囲に限定しました。また、`context.size` への早期アクセスによるエラーを避けるため、`LayoutBuilder` を導入し、ウィジェットのサイズ情報を安全に取得するように修正しました。

### 記憶の誕生エフェクト (4次元的演出) (完了)
- エントランス・アニメーション (フレア演出): UniverseCanvas内で、新しく表示範囲に入ったDiaryRecord（惑星）に対し、数フレームの間だけサイズと透明度をオーバーシュートさせる（輝きを強める）フレア演出を追加しました。
- 研磨度に応じた「重力感度」の調整: AnalysisReportの座標計算ロジック（_calculateRecordCoordinates）を修正し、引き寄せ係数をrecord.polishingLevelに応じて変化させるようにしました。研磨度が高い星ほど、エゴ状態の引力に対して「鋭敏に」反応し、座標の純度が高まるよう調整しました。

### 未知の感情タグの自動解釈レイヤー (完了)
- AI補完用インターフェースの追加: GeminiService に、新しいタグが5つのエゴ状態（CP, NP, A, FC, AC）のどれに最も近いかを判定する classifyNewTag メソッドを追加しました。
- フォールバック・ロジック: AnalysisReport でタグがマップに存在しない場合、即座にベースポジションに戻すのではなく、微小なランダムノイズ（宇宙のゆらぎ）を加えて、星が重ならないように配慮しました。

### 全ティア共通：初期粒度測定
- **初回起動時**: 53問（TEG 3準拠）の精密性格診断を実施。ユーザーの心の設計図（ベースライン）を確定させる。
- **目的**: どのティアに移行しても、過去からの蓄積を最高の精度で分析可能にする「ユーザーへの誠実さ」の担保。

### 機能ティア別のAI・広告ロジック
| ティア | 日々の記録時のAI | 分析・履歴画面でのAI | 性格診断（粒度調整） | 広告 |
| :--- | :--- | :--- | :--- | :--- |
| **Free** | 自動呼び出しなし | 月5回（週1+月1） | 3ヶ月毎（広告視聴） | バナー、リワード、インタースティシャル |
| **Tier 1** | 自動呼び出しなし | 1日1回 | 1ヶ月毎（自動） | なし |
| **Tier 2** | **毎回自動実行** | 無制限 | 随時（AIが必要と判断） | なし |